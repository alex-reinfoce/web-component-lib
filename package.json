{
  "name": "web-component",
  "version": "0.0.1",
  "scripts": {
    "build:component": "pnpm --filter component build",
    "dev:react": "pnpm --filter react-playground dev",
    "dev:vue": "pnpm --filter vue-playground dev",
    "playground": "concurrently \"pnpm run build:component\" \"pnpm run dev:react\" \"pnpm run dev:vue\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
